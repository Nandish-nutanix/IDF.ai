# Auto-scraped from IDF Try Me Editor (http://10.53.56.1:5002/try-me-editor)
# Total examples: 40
#==============================================================================

# Category: Queries - Where Clause
# Example: Contains
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "impact_type"
            }
          }
          operator: kContains
          rhs {
            leaf {
              value {
                str_list {
                  value_list: "Performance"
                }
              }
            }
          }
        }
      }
      query_name: "where_clause_kContains"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Not
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  where_clause {
    lhs { 
      comparison_expr {
        lhs {
          leaf {
            column: "disks"
          }
        }
        operator: kExists
      }
    }
    operator: kNot
  }
  query_name: "where_clause_kNot"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: In
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "m1"
            }
          }
          operator: kIN
          rhs {
            leaf {
              value {
                int64_list {
                  value_list: 0
                  value_list: 1
                }
              }
            }
          }
        }
      }
      query_name: "where_clause_kIN"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: GreaterThanEqual
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "cpu_usage"
            }
          }
          operator: kGE
          rhs {
            leaf {
              value {
               int64_value: 60
              }
            }
          }
        }
      }
      query_name: "where_clause_greater_than_equal"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: LessThan
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "cpu_usage"
            }
          }
          operator: kLT
          rhs {
            leaf {
              value {
               int64_value: 60
              }
            }
          }
        }
      }
      query_name: "where_clause_less_than"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Like: Match with multiple substrings
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "vdisk_capacity_list"
            }
          }
          operator: kLike
          rhs {
            leaf {
              value {
               str_value: ".*1000.*|.*900.*|.*800.*"
              }
            }
          }
        }
      }
      query_name: "where_clause_kLike_match_multiple_substrs"
    }
'''

Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Like: Match by substring
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "vdisk_capacity_list"
            }
          }
          operator: kLike
          rhs {
            leaf {
              value {
               str_value: ".*1000.*"
              }
            }
          }
        }
      }
      query_name: "where_clause_kLike_match_by_substr"
    }
'''

Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Equal
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "num_vcpus"
            }
          }
          operator: kEQ
          rhs {
            leaf {
              value {
               int64_value: 4
              }
            }
          }
        }
      }
      group_by {
        raw_columns {
          column: "vm_name"
        }
        raw_columns {
          column: "num_vcpus"
        }
      }
      query_name: "where_clause"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Like: Match by start of string
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "vdisk_capacity_list"
            }
          }
          operator: kLike
          rhs {
            leaf {
              value {
               str_value: "^1000.*"
              }
            }
          }
        }
      }
      query_name: "where_clause_kLike_match_by_start_of_str"
    }
'''

Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Exists
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "disks"
            }
          }
          operator: kExists
        }
      }
      query_name: "where_clause_kExists"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Nested Where Clause
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
#where clause can be nested using the boolean operators. Example : kAnd,
#kOr, kNot
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  where_clause {
    lhs { 
      comparison_expr {
        lhs {
          leaf {
            column: "node"
          }
        }
        operator: kNE
        rhs {
          leaf {
            value {
              str_value: "node_1"
            }
          }
        }
      }
    }
    operator: kAnd
    rhs {
      comparison_expr {
        lhs {
          leaf {
            column: "vm_name"
          }
        }
        operator: kExists   
      }
    }
  }
  query_name: "nested_where_clause"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Any
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "impact_type"
            }
          }
          operator: kAny
          rhs {
            leaf {
              value {
                str_list {
                  value_list: "Availability"
                  value_list: "Capacity"
                  value_list: "Configuration"
                  value_list: "Performance"
                  value_list: "SystemIndicator"
                }
              }
            }
          }
        }
      }
      query_name: "where_clause_kAny"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: Like: Match by start and end of string
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column : "vdisk_capacity_list"
            }
          }
          operator: kLike
          rhs {
            leaf {
              value {
               str_value: "^10.*00$"
              }
            }
          }
        }
      }
      query_name: "where_clause_kLike_match_by_start_and_end_of_str"
    }
'''

Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: NotEqual
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "node"
            }
          }
          operator: kNE
          rhs {
            leaf {
              value {
                str_value: "node_1"
              }
            }
          }
        }
      }
      query_name: "where_clause_not_equal"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: LessThanEqual
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "cpu_usage"
            }
          }
          operator: kLE
          rhs {
            leaf {
              value {
               int64_value: 60
              }
            }
          }
        }
      }
      query_name: "where_clause_less_than"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries - Where Clause
# Example: GreaterThan
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#where clause is used to filter out entities from the 
#response list based on the condition given in the query
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "cpu_usage"
            }
          }
          operator: kGT
          rhs {
            leaf {
              value {
               int64_value: 60
              }
            }
          }
        }
      }
      query_name: "where_clause_greater_than"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Group Limit
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#To group entities based on value given in 'group_by_column'
#'group_limit' can be set to control the number of groups
#returned in the response.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  group_by {
    group_by_column: "hypervisor_cpu_usage_ppm"
    group_by_column_type: kContinuous
    group_sort_order {
      sort_column: "hypervisor_cpu_usage_ppm"
    }
    raw_sort_order {
      sort_column: "num_vcpus"
    }
    raw_columns {
      column: "num_vcpus"
    }
    group_limit {
      limit: 10
      offset: 0
    }
    raw_limit {
      limit: 2
      offset: 10
    }
    num_buckets: 4
    bucket_boundary: 150
  }
  query_name: "group_and_raw_limit_and_offset"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Raw limit Queries
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#Used to limit the number of entities in the response.
#Offset denotes the index from which entities are fetches.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      group_by {
        raw_columns {
          column: "vm_name"
        }
        raw_limit {
            limit: 2
            offset: 4
        }
      }
      query_name: "limit_and_offset"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Group By
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#To group entities based on value given in group_by_column
arg = GetEntitiesWithMetricsArg()
query = '''
query {
    entity_list {
        entity_type_name: "vm" 
        
    }
    group_by {
        group_by_column: "cluster"
        raw_columns { 
            column: "_created_timestamp_usecs_" 
            
        }
        raw_sort_order {
            sort_column: "_created_timestamp_usecs_" 
            sort_order: kDescending 
            
        }
        raw_limit {
            limit: 10 
            offset: 0 
            
        }
    } 
    query_name: "prism:alert_group"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Downsampling
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#Applies aggregation using the downsampling operator
#to reduce the number of data points in the given interval
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      group_by {
        raw_columns {
          column: "num_io"
        }
        down_sampling_interval_secs: 300
      }
      query_name: "downsampling_interval"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Group Sort
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#To group entities based on value given in 'group_by_column'
#'group_sort_order' specifies the 'sort_column' with which the groups
#should be sorted and the order of sorting.
#NOTE: 'sort_column' should be 'group_by_column'
#or one of the 'aggregate_columns'
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  group_by {
    group_by_column: "hypervisor_cpu_usage_ppm"
    group_by_column_type: kContinuous
    aggregate_columns {
      column: "memory_usage_bytes"
      operator: kCount
    }
    group_sort_order {
      sort_column: "hypervisor_cpu_usage_ppm"
      sort_order: kAscending
    }
    bucket_interval_separators {
      int64_value: 1
    }
    bucket_interval_separators {
      int64_value: 2
    }
    bucket_interval_separators {
      int64_value: 3
    }
  }
  query_name: "group_sort"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Entity Specific Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#This is a point query. It is used to fetch
#attribute/stats of a particular entity.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
        entity_id: "3cec611e-106b-4d1f-bc4a-883337ba3dcb"
      }
      group_by {
        raw_columns {
          column: "vm_name"
        }
        raw_columns {
          column: "num_vcpus"
        }
      }
      query_name: "entity_specific_query"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Time Range Queries
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#'start_time_usecs' and 'end_time_usecs' specify the
#time range from which the metric values are to be fetched.
#If their values are not specified or if they are specified as 0,
#the latest metric values will be retrieved.
#NOTE: The maximum duration from present that is supported by the
#query is 5 years.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  group_by {
    raw_columns {
      column: "controller.read_source_oplog_bytes"
    }
  }
  start_time_usecs: 1571702400000000
  end_time_usecs:   1571702400020000
  query_name: "time_range_queries"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Lookup Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#This is a lookup query. First the outer query is executed following which
#for each lookup_query block, the inner query is run and a join is performed
#based on the conditions present in the block.
#Currently lookup queries have the following restrictions:
# - cannot have group_by_column
# - must have one lookup condition (ComparisonExpression for column
#   in the parent query and column in a lookup query)
# - the lookup condition can use only kEQ, kIN, kContains or kAny
# - the lookup condition can either be the only condition or the first
#   ComparisonExpression in the where_clause followed by kAND operator
# - the lookup condition must be on a raw_column
# - lookup attributes in the parent results must be singular values for
#   kContains operator (list values are ignored)
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list { entity_type_name: "vm" }
  group_by {
    raw_sort_order {
      sort_column: "cpu_usage"
      sort_order: kDescending
    }
    raw_limit { limit: 100 }
    raw_columns { column: "_entity_id_" }  # implementation needs it
    raw_columns { column: "cluster" }  # implementation needs it
    raw_columns { column: "name" }
    raw_columns { column: "cpu_usage" }

    lookup_query {
      entity_list { entity_type_name: "alert" }
      group_by {
        raw_limit { limit: 5 }
        raw_columns { column: "title" }
        raw_columns { column: "default_message" }
      }
      where_clause {
        lhs { comparison_expr {
          lhs { leaf { column: "vm" } }  # lookup column in inner query
          operator: kEQ
          rhs { leaf { column: "_entity_id_" } }  # lookup column in outer query
        } }
        operator: kAnd
        rhs {comparison_expr {
          lhs { leaf { column: "resolved" } }
          operator: kEQ
          rhs { leaf { value { bool_value: false } } }
        } }
      }
    }

    lookup_query {
      entity_list { entity_type_name: "cluster" }
      group_by {
        raw_columns { column: "name" }
      }
      where_clause {
        comparison_expr {
          lhs { leaf { column: "_entity_id_" } }  # lookup column in inner query
          operator: kEQ
          rhs { leaf { column: "cluster" } }  # lookup column in outer query
        }
      }
    }
  }
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Aggregate Columns
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#To group entities based on value given in 'group_by_column'
#'aggregate_columns' specifies the column that needs to be aggregated
#for each group of entities in the response, along with
#the operator to perform aggregation.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm" 
  }
  group_by {
    group_by_column: "vm_name"
    raw_columns { 
      column: "_created_timestamp_usecs_" 
    }
    aggregate_columns {
      column: "memory_usage_bytes"
      operator: kAvg
    }
    raw_sort_order {
      sort_column: "_created_timestamp_usecs_" 
      sort_order: kDescending 
    }
    raw_limit {
      limit: 10 
      offset: 0 
    }
  } 
  query_name: "aggregation"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Continuous Grouping
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#To group entities based on value given in 'group_by_column'
#The range of group-by column values are divided into the number
#of buckets specified by 'num_buckets' with the separator between
#the buckets specified by 'bucket_boundary'
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
      entity_type_name: "vm" 
  }
  group_by {
    group_by_column: "memory_usage_bytes"
    group_by_column_type: kContinuous
    group_sort_order {
      sort_column: "memory_usage_bytes"
    }
    raw_limit {
      limit: 10 
      offset: 0 
    }
    num_buckets: 3
    bucket_boundary: 70
  } 
  query_name: "continous_grouping"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Raw Sort
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#Sorts the entities according to the column and order
#mentioned in the raw_sort_order in the query
#Sort column can only be one of the raw columns
arg = GetEntitiesWithMetricsArg()
query = '''
 query {
      entity_list {
        entity_type_name: "vm"
      }
      group_by {
        raw_columns {
          column: "vm_name"
        }
        raw_columns {
          column: "num_vcpus"
        }
        raw_sort_order {
          sort_column: "vm_name"
          sort_order: kAscending
        }
      }
      query_name: "fetching_raw_columns_and_sorting"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Entity Count
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#To return total entity count in query response, set flags=2.
#The query only works for non-evictable type entities and wildcard
#queries.
#The flag will only return the entity count without any raw results
#or guids. If raw columns ae specified in the query arg, the flag
#would be deemed invalid.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      flags: 2
      query_name: "entity_count_query"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Having Clause Group By
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
# Having clause with group by query.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
    entity_list {
        entity_type_name: "vm" 
    }
    group_by {
        group_by_column: "cluster"
        having_clause {
          comparison_expr {
            lhs {
              leaf {
                column : "cluster"
              }
            }
            operator: kContains
            rhs {
              leaf {
                value {
                  str_list {
                    value_list: "UUID-0"
                  }
                }
              }
            }
          }
        }
    } 
    query_name: "having_clause_group_by_query"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Multi Dimensional Grouping
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#To achieve nested grouping based on more than one raw column 
#of an entity type, we use multidimensional grouping. The innermost grouping 
#will contain the entities that satisfy the raw column values of the groups.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
          entity_list {
            entity_type_name: "vm"
          }
          group_by {
            group_by_column: "memory_size_bytes"
            sub_group_by {
              group_by_column: "ip_addresses"
              raw_columns {
                column: "memory_size_bytes"
              }
              raw_columns {
                column: "ip_addresses"
              }
            }
          }
        }
'''
Merge(query, arg)
ret = insights_interface.GetEntitiesWithMetrics(arg)
print(ret)

#==============================================================================

# Category: Queries
# Example: Ancestor Join Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#For ancestor joins, the query response will contain the 'column'
#values of the ancestor in 'group_summaries'. The 'column'
#values aren't aggregated but instead are just the ancestor's
#'column' values.
#Note: Currently the 'ancestor_entity_type' can only be the
#'group_by_column' attribute.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  group_by {
    group_by_column: "cluster"
    aggregate_columns {
      column: "node"
      ancestor_entity_type: "cluster"
    }
    suppress_ancestor_tree_traversal: false
  }
  query_name: "ancestor_join_query"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Fetching Raw Columns
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1","2027")
#To fetch attribute/stats of all the entities
arg = GetEntitiesWithMetricsArg()
query = '''
query {
      entity_list {
        entity_type_name: "vm"
      }
      group_by {
        raw_columns {
          column: "vm_name"
        }
        raw_columns {
          column: "num_vcpus"
        }
      }
      query_name: "fetching_raw_columns"
    }
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Shard Ids
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#Only entities that belong to the shards specified
#will be returned in the response.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm"
  }
  group_by {
    raw_columns {
      column: "vm_name"
    }
    raw_columns {
      column: "num_vcpus"
    }
  }
  shard_id_list: "10"
  shard_id_list: "89"
  shard_id_list: "107"
  shard_id_list: "55"
  shard_id_list: "100"
  shard_id_list: "47"
  shard_id_list: "7"
  shard_id_list: "3"
  query_name: "shard_ids"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Grouping With Bucket Interval Separators
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
#To group entities based on value given in 'group_by_column'
#'bucket_interval_separators' is used to set user-defined buckets
#for continuous grouping. If set with values
#{v1, v2, v3 ... vn-1, vn }, the groups will be [int64_min, v1),
#[v1, v2), [v2, v3) .... [vn, int64_max].
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm" 
  }
  group_by {
    group_by_column: "hypervisor_cpu_usage_ppm"
    group_by_column_type: kContinuous
    raw_columns {
      column: "_entity_id_"
    }
    raw_sort_order {
      sort_column: "_entity_id_"
    }
    bucket_interval_separators {
      int64_value: 1
    }
    bucket_interval_separators {
      int64_value: 2
    }
    bucket_interval_separators {
      int64_value: 3
    }
    bucket_interval_separators {
      int64_value: 4
    }
  }
  query_name: "continous_grouping_with_bucket_intervals"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Namespaces
# Example: Where Clause Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
# where clause is used to filter out entities from the response list based on
# the condition given in the query.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm_test"
  }
  where_clause {
    comparison_expr {
      lhs {
        leaf {
          column: "namespace_attr_1"
          column_namespace: "np1"
        }
      }
      operator: kEQ
      rhs {
        leaf {
          value {
            str_value: "attr_value_1" 
          }
        }
      }
    }
  }
  query_name: "where_clause"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Namespaces
# Example: Group By Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
# To group entities based on value given in group_by_column.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm_test" 
  }
  group_by {
    group_by_column: "namespace_attr_1"
    group_by_column_namespace: "np1"
  } 
  query_name: "group_by"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Namespaces
# Example: Fetch Raw Columns Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
# To fetch namespace attributes of all the entities.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm_test"
  }
  group_by {
    raw_columns {
      column: "namespace_attr_1"
      column_namespace: "np1"
    }
  }
  query_name: "fetching_raw_columns"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Namespaces
# Example: Aggregation Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
# To group entities based on value given in 'group_by_column'.
# 'aggregate_columns' specifies the column that needs to be aggregated for 
# each group of entities in the response, along with the operator to perform
# aggregation.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm_test" 
  }
  group_by {
    group_by_column: "namespace_attr_1"
    group_by_column_namespace: "np1"
    aggregate_columns {
      column: "namespace_attr_2"
      column_namespace: "np2"
      operator: kAvg
    }
  } 
  query_name: "aggregation"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Namespaces
# Example: Group Sort Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly. 
insights_interface = InsightsInterface("127.0.0.1", "2027")
# To group entities based on value given in 'group_by_column'.
# 'group_sort_order' specifies the 'sort_column' with which the groups should
# be sorted and the order of sorting.
# NOTE: 'sort_column' should be 'group_by_column' or one of the
# 'aggregate_columns'.
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list {
    entity_type_name: "vm_test"
  }
  group_by {
    group_by_column: "namespace_attr_2"
    group_by_column_namespace: "np2"
    group_by_column_type: kDiscrete
    group_sort_order {
      sort_column: "namespace_attr_2"
      sort_column_namespace: "np2"
      sort_order: kAscending
    }
  }
  query_name: "group_sort"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Namespaces
# Example: Lookup Query
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
# Note: The default IP and port is the localhost IDF instance.
# You can configure it accordingly.
insights_interface = InsightsInterface("127.0.0.1", "2027")
# This is a lookup query on namespace attributes. First the outer query is
# executed following which for each lookup_query block, the inner query is run
# and a join is performed based on the conditions mentioned in the block.
# Currently lookup queries have the following restrictions:
#  - cannot have group_by_column
#  - must have one lookup condition (ComparisonExpression for column
#    in the parent query and column in a lookup query)
#  - the lookup condition can use only kEQ, kIN, kContains or kAny
#  - the lookup condition can either be the only condition or the first
#    ComparisonExpression in the where_clause followed by kAND operator
#  - the lookup condition must be on a raw_column
#  - lookup attributes in the parent results must be singular values for
#    kContains operator (list values are ignored)
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list { entity_type_name: "parent_etype"}
  group_by {
    raw_columns {
      column: "attr1_parent"
      column_namespace: "ns1"
    }
    raw_columns {
      column: "attr2_parent"
    }
    lookup_query {
      entity_list { entity_type_name: "lookup_etype"}
      group_by {
        raw_columns {
          column: "attr1_lookup"
          column_namespace: "ns2"
        }
        raw_columns {
          column: "atrr2_lookup"
        }
      }
      where_clause {
        comparison_expr {
          lhs {
            leaf {
              column: "attr1_lookup"   
              column_namespace: "ns2"
            }
          }
          operator: kEQ
          rhs {
            leaf {
              column: "attr1_parent"
              column_namespace: "ns1"
            }
          }
        }
      }
    }
  }
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Get Entities
# Example: By Entity Id
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = GetEntitiesArg()
query = '''
entity_guid_list {
  entity_type_name: "vm"
  entity_id : "1"
}
entity_guid_list {
  entity_type_name: "vm"
  entity_id : "2"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntities(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Get Entities
# Example: Multiple Entity Types
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = GetEntitiesArg()
query = '''
entity_guid_list {
  entity_type_name: "vm"
}
entity_guid_list {
  entity_type_name: "cluster"
}
entity_guid_list {
  entity_type_name: "node"
  entity_id : "1"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntities(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Get Entities
# Example: Including Deleted Entities
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = GetEntitiesArg()
query = '''
entity_guid_list {
  entity_type_name: "vm"
}
include_deleted_entities: true
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntities(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: BatchGetEntitiesWithMetric
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = BatchGetEntitiesWithMetricsArg()
query = '''
query_list {
  query {
    entity_list { entity_type_name: "cluster" }
    group_by { raw_columns { column: "_entity_id_" } }
    query_name: "fetching_cluster_entity_id"
  }
}
query_list {
  query {
    entity_list { entity_type_name: "vm" }
    where_clause {
      comparison_expr {
        lhs { leaf { column: "num_vcpus" } }
        operator: kEQ
        rhs { leaf { value { int64_value: 8 } } }
      }
    }
    group_by {
      raw_columns { column: "vm_name" }
      raw_columns { column: "num_vcpus" }
    }
    query_name: "where_clause"
  }
}
query_list {
  query {
    entity_list { entity_type_name: "disk" }
    flags: 2
    query_name: "entity_count_query"
  }
}
'''
Merge(query, arg)
try:
  ret = insights_interface.BatchGetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Queries
# Example: Cursor Query - Primary
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = GetEntitiesWithMetricsArg()
query = '''
query {
  entity_list { entity_type_name: "task" }
  group_by { }
  cursor_query_info {
    is_initial_cursor_query: true
    batch_size: 2
  }
  query_name: "primary_cursor_query"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntitiesWithMetrics(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Data Modification APIs
# Example: UpdateEntity CAS
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = UpdateEntityArg()
query = '''
entity_guid {
  entity_type_name: "vm"
  entity_id: "entity_id"
}
cas_value: 0
attribute_data_arg_list {
  attribute_data {
    name: "vm_name"
    value { str_value: "my_vm_name" }
  }
}
'''
Merge(query, arg)
try:
  ret = insights_interface.UpdateEntity(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Data Modification APIs
# Example: DeleteEntity NoCas
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = DeleteEntityArg()
query = '''
entity_guid {
  entity_type_name: "vm"
  entity_id: "1234"
}
'''
Merge(query, arg)
try:
  ret = insights_interface.DeleteEntity(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Register and Unregister APIs
# Example: RegisterMetricTypes
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = RegisterMetricTypesArg()
query='''
metric_type_list {
  is_attribute: true
  metric_name: "attr1"
  entity_type_name: "vm"
}
metric_type_list {
  is_attribute: false
  metric_name: "cpu_metric"
  entity_type_name: "vm"
  ttl_calculation: kCurrentTimeBased
  down_sampling_operator: kAvg
  down_sampling_interval_in_secs: 86400
}
'''
Merge(query, arg)
try:
  ret = insights_interface.RegisterMetricTypes(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================

# Category: Other Read APIs
# Example: GetEntityTypes
#------------------------------------------------------------------------------
import env
import gflags
gflags.FLAGS([])
from google.protobuf.text_format import *
from insights_interface.insights_interface_pb2 import *
from insights_interface.insights_interface import *
insights_interface = InsightsInterface("127.0.0.1", "2027")
arg = GetEntityTypesArg()
query = '''
entity_type_name: "vm"
'''
Merge(query, arg)
try:
  ret = insights_interface.GetEntityTypes(arg)
  print(ret)
except InsightsInterfaceError as ex:
  print("Error Message: " + ex.message + "\n")
  print(ex.ret)

#==============================================================================
