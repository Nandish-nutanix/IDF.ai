fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.31.0
chromadb>=0.4.0

# --- ML inference / fine-tuning (Apple Silicon, Microsoft Phi-4 only) ---
# MLX runtime + Phi-4 serving and LoRA/GRPO fine-tuning.
mlx-lm>=0.19.0
# Grammar-constrained JSON decoding (QueryIR) on MLX. Optional but recommended;
# the pipeline degrades gracefully (validate->repair->fallback) if absent.
outlines>=0.1.0
# huggingface_hub is needed to download the Phi-4 base for (re)training.
huggingface_hub>=0.23.0
